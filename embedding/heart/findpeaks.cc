
#include "findpeaks.h"

namespace findpeaks {



  PeakList findpeaks(std::vector<float> signal, int min_dist, float thres,
		     std::string direction, int energy_window) {
    PeakList j_peaks;
    std::vector<float> diff1_signal = diff_signal(signal);
    
    
    
    return j_peaks;
  }

} 




